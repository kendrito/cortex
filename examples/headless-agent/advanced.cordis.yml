# Add Code Mode and Cordis tools to the headless spawn/workflow stack.
- id: base
  name: '@cortex/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - id: agent-spine
        name: '@cortex/agent-spine-demo'
        config:
          agents:
            - id: main
              provider: cortex-official
              model: cortex-v4-pro
              cwd: !!js process.cwd()
          workspaceContext:
            maxBytes: 65536
          tools:
            mode: both
          persona: |
            You are headless-agent, a coding assistant powered by the {{model}} model. Your working directory is {{cwd}}.

            Verify your work by running the code or tests. Keep answers brief and factual.
      - id: persistence
        name: '@cortex/session-persistence-jsonl'
        config:
          root: './.sessions'
          compression: !!js "process.env.CORTEX_SNAPSHOT === undefined ? 'zstd' : 'none'"
      - insert:
          - id: code-runtime
            name: '@cortex/code-runtime-worker-thread'
          - id: cordis-host-runner
            name: '@cortex/cordis-host-runner'
          - id: tool-cordis
            name: '@cortex/tool-cordis'
