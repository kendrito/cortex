/**
 * Client-namespace outlet of `@cortex/atlassian`: the pure type vocabulary
 * (records, projection view, Remote requests/results) for the browser half.
 * Zero values, so a client program never pulls host Context merges.
 *
 * @module @cortex/atlassian/client
 */

export type * from './types.ts'
