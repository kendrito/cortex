[build-system]
requires = ["hatchling==1.30.1"]
build-backend = "hatchling.build"

[project]
name = "cortex-sdk"
version = "0.0.0.dev0"
description = "Python SDK for Cortex"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "Cortex" }]
dependencies = [
  "pydantic>=2.12,<3",
  "cortex-runtime-bin==0.0.0.dev0",
]

[project.urls]
Homepage = "https://github.com/local/cortex"
Documentation = "https://github.com/local/cortex/blob/master/docs/user/guide/python-sdk.md"
Issues = "https://github.com/local/cortex/issues"
Source = "https://github.com/local/cortex"

[dependency-groups]
test = ["pytest>=8.0"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.hatch.build.targets.wheel]
packages = ["src/cortex_harness"]

# Editable installs see runtime executables injected after installation instead
# of freezing a wheel snapshot.
[tool.uv.sources]
cortex-runtime-bin = { path = "../sdk-runtime", editable = true }
